on: push

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      # ... other steps ...
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: python